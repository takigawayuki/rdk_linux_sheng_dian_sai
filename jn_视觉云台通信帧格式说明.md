# 视觉与云台通信协议帧格式说明

## 概述

本工程使用了两套独立的通信协议来传递视觉数据和控制指令：

1. **板间通信协议 (Board Comm)** - 用于状态切换控制
2. **视觉数据协议 (OpenCV Vision)** - 用于目标跟踪

---

## 协议一：板间通信协议 (Board Comm)

### 文件位置
- `diansai_yuntai_mc02-yuntai_karman/modules/timachine/board_comm.h`
- `diansai_yuntai_mc02-yuntai_karman/modules/timachine/board_comm.c`

### 帧格式定义

```c
#define BOARD_COMM_FRAME_SIZE 22u   // 总帧大小
#define BOARD_COMM_DATA_SIZE  16u   // 数据部分大小

typedef struct
{
    uint8_t header[2];          // 帧头: 0x55 0xAA
    int32_t int_data[2];        // 2个int32整型值 (8字节)
    float float_data[2];        // 2个float浮点型值 (8字节)
    uint32_t checksum;          // 简单累加校验和 (4字节)
} __attribute__((packed)) board_comm_packet_t;
```

### 帧结构示意图 (共22字节)

| 位置 | 大小 | 内容 |
|------|------|------|
| 0-1  | 2 字节 | 帧头 `0x55 0xAA` |
| 2-5  | 4 字节 | `int_data[0]` (控制题号) |
| 6-9  | 4 字节 | `int_data[1]` (子状态/阶段) |
| 10-13| 4 字节 | `float_data[0]` (运动进度/距离) |
| 14-17| 4 字节 | `float_data[1]` (运动速度) |
| 18-21| 4 字节 | 校验和 (前面16字节的累加和) |

### 数据含义

#### `int_data[0]`: 判断题号
| 值 | 含义 |
|----|------|
| 1 | 云台停止/准备 |
| 2 | 题号2执行 |
| 3 | 题号3执行 |
| 4, 5 | 题号4-1执行 |
| 6 | 题号4-3执行 |

#### `int_data[1]`: 子状态 (控制状态机流转)
| 值 | 含义 |
|----|------|
| 0 | 等待 (WAITING) |
| 1 | 转向/准备 (TURING) |
| 2 | 跟踪运行 (RUNNING) |

#### `float_data[0]`: 运动进度 (0-100表示0%-100%)
在题号4-1/4-3中用于判断转向区域：
- 0-12.5: 区域1
- 12.5-37.5: 区域2
- 37.5-62.5: 区域3
- 62.5-87.5: 区域4

#### `float_data[1]`: 运动速度
- >= 600.0 时进入"转向状态" (`no4_turning_status = 1`)

### 校验和算法

使用**简单的累加和校验**（非CRC）：

```c
static uint32_t board_comm_checksum(uint8_t *data, uint8_t len)
{
    uint32_t sum = 0;
    for (int i = 0; i < len; i++)
    {
        sum += data[i];  // 只是简单地将所有字节相加
    }
    return sum;
}
```

---

## 协议二：视觉数据协议 (OpenCV Vision)

### 文件位置
- `diansai_yuntai_mc02-yuntai_karman/modules/opencv/opencv.h`
- `diansai_yuntai_mc02-yuntai_karman/modules/opencv/opencv.c`

### 帧格式定义

```c
#define FRAME_SOF          0xA5
#define FRAME_PAYLOAD_LEN  (2+2+4*12)  // 52 字节
#define FRAME_FULL_LEN     (1+1+FRAME_PAYLOAD_LEN) // 54 字节
```

### 帧结构示意图 (共54字节)

| 位置 | 大小 | 内容 |
|------|------|------|
| 0 | 1 字节 | 帧头 `0xA5` |
| 1 | 1 字节 | Payload长度 (固定52) |
| 2-3 | 2 字节 | `cmd_id` (命令ID, 小端) |
| 4-5 | 2 字节 | `flags` (状态标志, 小端) |
| 6-53 | 48 字节 | 12个float数据 |

### 12个float数据的含义

| 索引 | 含义 | 单位/范围 | 用途 |
|------|------|----------|------|
| 0 | yaw误差 | ±100 | 云台yaw方向控制 |
| 1 | pitch误差 | ±100 | 云台pitch方向控制 |
| 2 | 目标距离 | 500-1560mm | 调整PID增益系数 |
| 3-11 | 保留 | - | 暂未使用 |

### 数据解析示例

```c
// 解析12个float
for (int i = 0; i < 12; i++) {
    memcpy(&opencv_Vision_data[i], buf + 6 + 4*i, sizeof(float));
}
```

---

## 数据流向图

```
┌─────────────────┐      UART 22字节帧       ┌─────────────────┐
│  主控/计算板     │ ────────────────────────▶│  云台控制板      │
│  (发送指令)      │   board_comm协议         │  (执行控制)      │
│                 │                          │                 │
│  - 题号切换     │   int_data[0] = 题号     │  - 状态机判断   │
│  - 子状态控制   │   int_data[1] = 阶段     │  - 切换模式     │
│  - 运动进度     │   float_data[0] = 进度  │  - 控制云台     │
│  - 速度反馈     │   float_data[1] = 速度  │                 │
└─────────────────┘                          └─────────────────┘
        ▲                                              │
        │ 54字节帧                                      │
        │ opencv协议                                    │
        │ (视觉跟踪数据)                                │
┌─────────────────┐                                     │
│  视觉处理模块    │ ──────────────────────────────────┘
│  (目标识别)      │   opencv_Vision_data[0..2] 用于     │
│                 │   yaw/pitch的PID控制                 │
└─────────────────┘
```

---

## 协议对比总结

| 协议 | 帧大小 | 作用 | 关键数据 | 校验方式 |
|------|--------|------|----------|----------|
| **Board Comm** | 22字节 | 控制题号和状态机切换 | `int_data[0]`(题号), `int_data[1]`(阶段) | 累加和 |
| **OpenCV Vision** | 54字节 | 提供目标跟踪误差 | `data[0]`(yaw误差), `data[1]`(pitch误差), `data[2]`(距离) | 帧头+长度校验 |

---

## 校验方式说明

本工程使用的**累加和校验**与CRC校验的对比：

| 特性 | 累加和校验 (当前使用) | CRC校验 |
|------|---------------------|---------|
| 实现复杂度 | 简单（循环加法） | 复杂（位运算+多项式） |
| 检错能力 | 较弱 | 强 |
| 计算开销 | 低 | 较高 |
| 常用程度 | 常用于简单协议 | 常用于工业级协议 |

**累加和校验的缺点**：
- 无法检测字节顺序互换（如`0x01 0x02`变成`0x02 0x01`，和相同但数据错误）
- 无法检测某些特定错误模式（如两个字节同时加1和减1）

**适用场景**：
在比赛/原型项目中，累加和校验实现简单且能满足基本的数据完整性检查需求。