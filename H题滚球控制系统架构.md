# H题车载平衡滚球控制系统架构

> 硬件补充：实际摆杆执行器是步进电机。本文前面使用的“舵机/角度执行层”描述保留用于说明通用控制结构；在本系统中，应将其具体实现理解为“步进电机、驱动器、机械传动和摆杆零点共同组成的角度执行层”。详细换算见第 16 节。

## 1. 一句话说明

这个系统可以概括为：

```text
视觉位置反馈 + PID位置纠偏 + 小车加速度前馈补偿 + 舵机摆杆执行
```

摄像头负责测量钢球相对导轨的位置和速度；位置控制器根据目标位置产生反馈摆角；车辆加速度前馈提前补偿小车运动造成的惯性扰动；舵机根据最终角度指令控制摆杆，使钢球相对小车稳定在目标位置。

## 2. 完整控制链路

```text
摄像头采集图像
    ↓
镜头畸变矫正
    ↓
钢球检测，得到像素位置 (u, v)
    ↓
像素/厘米标定映射
    ↓
钢球相对导轨的位置 x
    ↓
滤波器估计位置 x、速度 v、时间戳和置信度
    ↓
控制器计算位置误差 e = x_target - x
    ↓
PID/PD 计算反馈角 θPID
    ↓
与车辆加速度前馈角 θFF 相加
    ↓
角度限幅、变化率限制和安全检查
    ↓
发送目标角度 θcmd 给摆杆舵机
    ↓
摆杆倾斜，钢球运动
    ↓
摄像头再次测量，形成闭环
```

## 3. 视觉模块应该发送什么

视觉模块最好不要直接发送“位置偏差”，而是发送测量状态：

```text
position_cm          钢球相对导轨中心的位置
velocity_cm_s        滤波估计的相对速度
timestamp            图像采集或检测时间戳
confidence           检测置信度
detected             当前帧是否检测成功
predicted            是否使用滤波预测值
sequence             数据序号
```

控制目标 `x_target` 应由控制器或任务状态机管理。控制器自己计算：

```text
e = x_target - position_cm
```

这样可以保持职责清晰：

- 视觉模块只负责“钢球现在在哪里”。
- 任务状态机负责“钢球应该去哪里”。
- 控制器负责“怎样让钢球到达目标”。

如果视觉直接计算偏差，它就必须同时知道目标位置，后续切换 `O、+5 cm、-5 cm` 或任意指定位置时会增加模块耦合。

## 4. PID反馈控制

定义：

```text
x_target    钢球目标位置
x           钢球当前相对位置
v           钢球当前相对速度
e           位置误差
```

位置误差为：

```text
e = x_target - x
```

控制器可以写为：

```text
θPID = Kp·e + Ki∫e dt - Kd·v
```

这里使用 `-Kd·v`，是因为目标位置通常是常数，误差的导数近似为：

```text
de/dt ≈ -v
```

三个参数的作用：

- `Kp`：位置偏差越大，给出越大的纠偏角。
- `Ki`：消除摩擦、坡度和恒定扰动造成的稳态误差。
- `Kd`：根据钢球速度提供阻尼，减少超调和振荡。

视觉位置存在像素噪声，因此不建议直接对原始位置做简单差分作为 D 项。应优先使用 α-β 滤波或卡尔曼滤波估计速度。

## 5. 为什么需要加速度前馈

摆杆安装在运动小车上。小车向前加速时，钢球因为惯性会相对小车向后运动。

普通 PID 的过程是：

```text
小车开始加速
    ↓
钢球先发生位置偏移
    ↓
摄像头检测到误差
    ↓
PID 再倾斜摆杆纠正
```

带积分项的 PID 最终能够抵消近似恒定的小车加速度，但它必须先积累误差。车辆加速变化快时，容易出现明显瞬时偏差、积分累积和摆杆角度饱和。

加速度前馈的过程是：

```text
检测或预知小车加速度
    ↓
立即计算理论补偿角
    ↓
在钢球明显偏移前倾斜摆杆
```

因此：

- PID 是误差发生后的反馈纠正。
- 前馈是根据可测扰动提前补偿。
- 前馈不能替代 PID，PID 仍要修正模型误差和未知扰动。

## 6. 车载滚球物理模型

定义：

```text
x              钢球相对导轨的位置
a_car          小车沿导轨方向的加速度
θ              导轨角度
g              重力加速度
m              钢球质量
R              钢球半径
I              钢球转动惯量
```

正方向约定：

- 小车前进方向为 `+x`。
- 导轨 `+x` 一端降低时，`θ` 为正。
- 正导轨角会让重力推动钢球向 `+x` 滚动。

实心钢球转动惯量为：

```text
I = 2/5 mR²
```

在随小车加速的坐标系中，钢球相对导轨的滚动方程为：

```text
              g sinθ - a_car cosθ
x_ddot =  -----------------------------  - c x_dot
                  1 + I/(mR²)
```

代入实心钢球转动惯量：

```text
x_ddot = 5/7 [g sinθ - a_car cosθ] - c x_dot
```

其中 `c` 表示等效滚动阻尼。

## 7. 前馈角怎么计算

希望钢球相对小车保持不动，需要满足：

```text
x_dot = 0
x_ddot = 0
```

忽略阻尼后令相对加速度为零：

```text
g sinθFF - a_car cosθFF = 0
```

得到理论前馈角：

```text
θFF = atan(a_car/g)
```

例如：

```text
a_car = 1.0 m/s²
g = 9.80665 m/s²

θFF = atan(1.0/9.80665)
θFF ≈ 5.82°
```

实际系统可以增加前馈比例：

```text
θFF = Kff·atan(a_car/g)
```

建议从较小的 `Kff` 开始，通过实车实验逐渐增加到接近 `1`。

## 8. 最终角度指令

PID反馈角和加速度前馈角相加：

```text
θcmd = θPID + θFF
```

加入完整表达式：

```text
e = x_target - x

θPID = Kp·e + Ki∫e dt - Kd·v

θFF = Kff·atan(a_parallel/g)

θcmd = limit(θPID + θFF, -θmax, +θmax)
```

其中 `a_parallel` 是车辆加速度在导轨方向上的分量：

```text
a_parallel = a_vehicle · e_rod
```

最终还应增加：

- 摆杆最大角度限制。
- 角度变化率限制。
- 积分抗饱和。
- 视觉数据超时保护。
- 钢球丢失保护。
- 舵机通信异常保护。

## 9. 球的加速度是否要等于车的加速度

需要区分参考系。

三个加速度分别是：

```text
a_car       小车相对地面的加速度
a_relative  钢球相对导轨的加速度
a_ball      钢球相对地面的加速度
```

在导轨角度变化较慢的简化模型中：

```text
a_ball,x = a_car + a_relative cosθ
```

钢球稳定保持在目标位置时：

```text
x_relative = x_target
v_relative = 0
a_relative = 0
```

因此：

```text
a_ball,x = a_car
```

所以“球的水平加速度等于小车加速度”是钢球已经稳定跟随小车时的结果。

在钢球偏离目标、需要滚回目标点的过程中：

```text
a_relative ≠ 0
```

这时钢球与小车的加速度不应完全相等，否则钢球相对小车的位置永远不会改变，也就不能回到目标点。

## 10. 小车加速度怎么获得

### 10.1 编码器

根据相邻时刻的小车速度计算：

```text
a_car ≈ (v_car[k] - v_car[k-1]) / Δt
```

速度差分会放大编码器量化噪声，需要低通滤波。

### 10.2 IMU

IMU 可以更快感知加速和减速，但不能直接使用原始单轴数值。需要处理：

- 加速度计零偏。
- IMU 安装角度。
- 重力在各轴上的投影。
- 车体振动。
- 高频噪声。
- 时间戳和通信延迟。

### 10.3 运动控制指令

如果小车控制器已经有速度规划，可以直接使用规划加速度作为前馈。它没有传感器测量延迟，但不能反映打滑、碰撞和实际电机响应。

更可靠的方案是融合：

```text
运动规划加速度 + 编码器 + IMU
```

## 11. 舵机内环和钢球位置外环

滚球控制器通常可以理解为两层：

```text
钢球位置外环
    ↓
计算目标摆杆角度 θcmd
    ↓
舵机角度内环
    ↓
实际摆杆角度 θ
```

位置外环不能假设舵机会瞬间达到目标角度。真实舵机存在速度限制、回差、死区和通信延迟。

如果可以测量实际摆杆角度，应将实际角度反馈给控制器；如果不能测量，至少应在模型中考虑舵机的一阶响应和最大变化率。

## 12. 推荐的软件模块边界

```text
视觉模块
├── 相机采集
├── 畸变矫正
├── 钢球检测
├── 像素/厘米映射
└── 位置速度滤波

控制模块
├── 目标位置管理
├── PID位置反馈
├── 小车加速度前馈
├── 角度限幅和抗饱和
└── 丢球与超时保护

执行模块
├── 舵机通信
├── 角度内环
└── 硬件故障处理
```

## 13. 推荐的数据流

```text
VisionState {
    position_cm
    velocity_cm_s
    confidence
    detected
    predicted
    timestamp
    sequence
}

VehicleState {
    velocity_m_s
    acceleration_m_s2
    yaw_rate_rad_s
    timestamp
}

ControlTarget {
    target_position_cm
    enabled
}

RodCommand {
    target_angle_deg
    sequence
    control_state
}
```

这些数据必须带时间戳。控制器应拒绝过期的视觉位置和车辆加速度，不能把旧数据继续用于高速闭环。

## 14. 调试顺序

推荐按下面的顺序调试：

1. 小车静止，关闭加速度前馈。
2. 只调整 `Kp、Kd`，使钢球可以稳定回到中心。
3. 加入较小 `Ki`，消除固定坡度和摩擦造成的稳态误差。
4. 限制最大摆杆角度并加入积分抗饱和。
5. 让小车进行低加速度直线运动，记录钢球误差。
6. 确认加速度正方向和摆杆补偿方向正确。
7. 从较小 `Kff` 开始加入加速度前馈。
8. 比较开启和关闭前馈时的最大位置误差。
9. 增加加速、减速、转弯和振动测试。
10. 最后验证视觉丢球、通信超时和舵机异常保护。

不要同时盲调 PID 和前馈，否则很难判断问题来自反馈参数、加速度方向、加速度延迟还是物理模型误差。

## 15. 当前 Workbench 中的对应实现

- `src/main.ts`：控制面板、二维受力图、前馈开关和实时读数。
- `src/simulation.ts`：滚球动力学、PID、加速度前馈和舵机一阶响应。
- `src/scene.ts`：三维摆杆、钢球和小车加速度方向显示。
- `README.md`：平台操作方法和物理模型详细说明。

Workbench 当前使用理想加速度输入和简化物理模型，适合教学、验证控制方向和比较有无前馈。真实系统参数仍需通过实际小车、钢球、摆杆和舵机实验辨识。

## 16. 步进电机执行器与控制单位补充

### 16.1 为什么可以把PID反馈角和前馈角相加

控制系统中的加法节点只能相加单位相同的物理量：

```text
角度 + 角度          可以
加速度 + 加速度      可以
角度 + 加速度        不可以
```

PID本身没有固定输出单位。PID输出什么，取决于控制器设计时把它定义成什么。

当前Workbench和本文前面的主要方案采用“角度域控制”：

```text
位置误差 e              cm
钢球速度 v              cm/s
PID输出 θPID            °
前馈输出 θFF            °
最终目标角 θcmd         °
```

PID公式为：

```text
θPID = Kp·e + Ki∫e dt - Kd·v
```

为了让结果单位是角度，各参数单位应为：

```text
Kp：°/cm
Ki：°/(cm·s)
Kd：°·s/cm
```

例如：

```text
e = 1 cm
Kp = 0.75 °/cm

θP = Kp·e = 0.75°
```

车辆加速度不能直接和这个 `0.75°` 相加，必须先通过物理模型换算成角度：

```text
a_car = 1.0 m/s²

θFF = atan(a_car/g)
     = atan(1.0/9.80665)
     ≈ 5.82°
```

此时两项都是角度，可以相加：

```text
θcmd = θPID + θFF
     = 0.75° + 5.82°
     = 6.57°
```

所以“PID反馈角和加速度前馈角相加”中的“加速度前馈角”，不是加速度本身，而是由小车加速度换算得到的理论摆杆角度。

### 16.2 相加后的角度给谁

在本系统中，`θcmd` 不是发送给小车行驶电机，也不是直接作为一个浮点数写入步进电机线圈。

它先交给摆杆的步进电机执行层：

```text
θcmd 目标摆杆角度
    ↓
角度限幅和变化率限制
    ↓
摆杆角度到电机位置的机械反解
    ↓
目标电机步数 Ntarget
    ↓
计算方向 DIR 和剩余步数 ΔN
    ↓
规划STEP脉冲频率和加减速
    ↓
步进电机驱动器
    ↓
步进电机转动
    ↓
机械传动带动摆杆达到目标角度
```

因此真正的最终接收者是：

```text
步进电机位置/脉冲规划器
```

步进驱动器通常接收：

```text
DIR          旋转方向
STEP         每个脉冲前进一步或一个微步
ENABLE       驱动器使能
```

控制器需要把角度目标转换成这些数字信号。

### 16.3 摆杆角度如何换算成步数

先定义：

```text
Nmotor       电机每转整步数，例如200步/转
μ            微步细分数，例如16
i            传动比：电机转数/摆杆转数
θzero        摆杆机械零点对应角度
θcmd         目标摆杆角度
```

如果电机通过齿轮或同步带线性驱动摆杆，每度需要的脉冲数为：

```text
Kstep = Nmotor·μ·i / 360
```

目标脉冲位置为：

```text
Ntarget = round[(θcmd - θzero)·Kstep]
```

当前估计位置为 `Ncurrent` 时：

```text
ΔN = Ntarget - Ncurrent
```

方向和步数为：

```text
DIR = sign(ΔN)
需要发送的STEP脉冲数 = |ΔN|
```

例如：

```text
Nmotor = 200步/转
μ = 16微步
i = 3

Kstep = 200×16×3/360
      ≈ 26.67脉冲/度

θcmd = 6.57°
θzero = 0°

Ntarget ≈ round(6.57×26.67)
        ≈ 175个脉冲
```

这个例子只用于说明换算。实际 `i`、正方向和零点必须根据机械结构测量。

如果使用连杆、偏心轮、丝杆或摇臂，电机角度和摆杆角度可能不是线性关系，不能直接使用固定 `Kstep`。这时应建立：

```text
θrod = f(θmotor)
```

控制时使用反函数：

```text
θmotor_cmd = f⁻¹(θrod_cmd)
```

工程上可以用机构几何公式，也可以实测多个角度点后建立查表和插值。

### 16.4 脉冲数和脉冲频率不是一回事

步进电机控制中：

```text
脉冲总数        决定最终转过多少角度
脉冲频率        决定转动速度
脉冲频率变化率  决定电机加速度
```

只计算 `Ntarget` 还不够。如果瞬间以很高频率发送所有脉冲，步进电机可能失步、振动或使摆杆产生冲击。

必须限制：

- 最大摆杆角度。
- 最大电机步频。
- 最大电机加速度。
- 摆杆角速度。
- 摆杆角加速度。

推荐使用梯形速度或S形速度规划：

```text
低频启动
    ↓
逐渐提高STEP频率
    ↓
保持允许的最大频率
    ↓
接近目标位置时逐渐降低频率
    ↓
停在Ntarget
```

滚球位置外环会不断更新 `θcmd`，步进电机规划器应始终追踪最新目标，而不是每次阻塞执行完旧目标后才读取新命令。

### 16.5 步进电机是否存在角度内环

普通开环步进电机没有真正的角度反馈。控制器通常根据已经发送的STEP脉冲累计 `Ncurrent`，假设电机没有失步：

```text
估计角度 = 累计脉冲数 / Kstep
```

如果电机因为负载过大、加速度过高或机械卡滞发生失步，软件估计角度与真实摆杆角度会逐渐不一致。

因此系统至少需要：

- 上电回零开关或光电限位，用于建立机械零点。
- 摆杆正负角度机械限位。
- 最大步频和加速度限制。
- 驱动器故障检测。
- 定期重新回零或零点一致性检查。

更可靠的方案是在摆杆轴或步进电机轴增加编码器，形成闭环步进系统：

```text
θcmd
    ↓
步进位置控制器
    ↓
STEP/DIR
    ↓
步进电机和摆杆
    ↓
编码器测量 θactual
    └────────反馈到位置控制器
```

这时才存在真正的执行器角度内环。

### 16.6 角度域控制的最终结构

结合步进电机后，推荐结构为：

```text
摄像头
    ↓
钢球位置 x、速度 v、时间戳、置信度
    ↓
e = x_target - x
    ↓
θPID = Kp·e + Ki∫e dt - Kd·v

车辆加速度 a_parallel
    ↓
θFF = Kff·atan(a_parallel/g)

θPID + θFF
    ↓
θcmd角度限幅、积分抗饱和和变化率限制
    ↓
机械反解 θcmd → Ntarget
    ↓
步进电机轨迹规划器
    ↓
DIR + STEP + ENABLE
    ↓
步进驱动器和步进电机
    ↓
摆杆角度变化
    ↓
钢球运动
    ↓
摄像头重新测量
```

这里存在两个不同的闭环概念：

1. 钢球位置外环一定存在，由摄像头测量钢球位置形成闭环。
2. 步进电机角度内环只有增加编码器后才真正存在；普通开环步进只能依靠脉冲计数估算位置。

### 16.7 也可以采用加速度域控制

“先把加速度相加”的理解也可以形成另一种控制器。

让PID输出钢球期望相对加速度：

```text
aPID = Kp·e + Ki∫e dt - Kd·v
```

此时：

```text
aPID单位为m/s²
```

PID参数单位应相应改变。假设位置和速度统一使用SI单位：

```text
e：m
v：m/s
aPID：m/s²

Kp：1/s²
Ki：1/s³
Kd：1/s
```

这时先在加速度层处理控制需求，再通过滚球逆模型换算成角度。滚球模型为：

```text
a_relative = [g sinθ - a_car cosθ]/(1 + I/mR²) - c·v
```

希望实现 `a_relative = aPID`，则应求解：

```text
g sinθ - a_car cosθ
    = (1 + I/mR²)(aPID + c·v)
```

对于实心球 `I/mR²=2/5`：

```text
g sinθ - a_car cosθ
    = 7/5(aPID + c·v)
```

令：

```text
A = 7/5(aPID + c·v)
δ = atan2(a_car, g)
G = sqrt(g² + a_car²)
```

则可以写成：

```text
G sin(θ - δ) = A
```

得到目标角度：

```text
θcmd = δ + asin(A/G)
```

当 `aPID=0、v=0` 时：

```text
θcmd = δ = atan2(a_car, g)
```

这就退化为前面使用的理论前馈角。

加速度域控制的完整流程是：

```text
位置误差
    ↓
PID计算期望相对加速度aPID
    ↓
结合小车加速度和滚球模型
    ↓
逆模型计算θcmd
    ↓
θcmd换算成步进电机目标步数
    ↓
轨迹规划后发送STEP/DIR
```

角度域和加速度域都可以使用。两者的区别是：

- 角度域更简单，容易在比赛现场调参。
- 加速度域物理意义更直接，但更依赖模型、单位一致性和加速度质量。

无论采用哪一种，最终都必须得到摆杆目标角度，再换算成步进电机的目标位置和脉冲序列。

### 16.8 步进电机版本的安全处理

步进电机滚球系统至少应实现以下保护：

1. 上电时先低速回零，成功后才允许进入滚球闭环。
2. `θcmd` 超过机械安全角时立即限幅。
3. 视觉数据超时或丢球时停止积分，摆杆以受限速度回到安全角。
4. 小车加速度数据过期时关闭或逐渐衰减前馈，不能保持旧补偿角。
5. 目标步数变化过快时经过轨迹规划，不直接跳变步频。
6. 到达软限位后禁止继续向同方向发送脉冲。
7. 急停时先禁止新的控制目标，再按机械安全要求决定保持使能还是卸力。
8. 如果有编码器，应检测指令角与实际角的持续偏差并报告失步。

特别需要注意：步进电机停止发送脉冲并不一定等于机械安全。驱动器保持使能时会产生保持力矩和发热；关闭使能后摆杆可能在重力作用下自由下落。急停策略必须结合实际机械结构确定。

### 16.9 Workbench模型如何理解

当前Workbench中的“一阶执行器响应”原本用来近似通用舵机延迟。对于实际步进电机，应把它理解为下面这些因素合并后的简化结果：

```text
步数取整
+ 最大步频
+ 脉冲加减速
+ 机械传动惯量
+ 摆杆结构响应
```

后续若要提高仿真真实性，应将通用一阶响应替换为明确的步进电机模型，至少加入：

- 电机每转步数。
- 微步细分。
- 机械传动比或连杆几何。
- 最大步频。
- 最大步频变化率。
- 脉冲位置累计。
- 回零状态。
- 可选失步和编码器反馈模型。

在完成这些参数测量前，保留简化执行器模型更适合验证滚球控制方向，但不能用它预测真实步进电机的极限响应速度。
