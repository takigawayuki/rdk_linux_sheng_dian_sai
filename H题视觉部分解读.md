# H题“车载平衡滚球运动控制系统”视觉部分解读

> 依据：工程根目录中的 `H题_车载平衡滚球运动控制系统.pdf`（2026 年赛区赛 H 题）。  
> 本文只重点解读视觉相关要求，并结合当前 `xbhdcc_camera` 工程给出可落地的实现建议。

## 1. 先给结论

这道题中有两套感知系统，不能混用：

1. **小车循迹**：只能使用红外光电模块，数量不限。不能用摄像头循迹。
2. **钢球位置检测**：必须使用摄像头。摄像头既要为平衡控制器实时提供钢球位置，又要完成清晰、稳定、可记录回放的图传。

视觉系统真正要解决的核心问题是：

> 将每一帧图像中的钢球中心位置，稳定、低延迟地换算成相对于摆杆中心点 O 的一维坐标 `x`（单位 cm），提供给摆杆闭环控制器；同时把带有钢球轨迹和位置标注的画面实时传到场外设备并完整录像。

视觉输出不应只是“检测到了球”，而应至少包含：

```text
timestamp, x_cm, velocity_cm_s, confidence, detected
```

其中控制器最重要的输入是 `x_cm`，速度 `velocity_cm_s` 可由滤波器估算，用于改善动态控制效果。

## 2. 题面中与视觉直接相关的硬性要求

### 2.1 图传要求（基础要求第 1 项，6 分）

- 图传装置由发送模块和接收模块组成。
- 发送模块必须稳固安装在小车上。
- 接收模块及显示、存储设备必须整体位于环形线路外。
- 必须稳定、实时显示钢球在凹槽中滚动的画面。
- 每次测试都要完整录像，并能按要求回放。
- 接收模块及其连接的 PAD、笔记本电脑等，赛后会和作品一起封存。

这意味着不能只在车载端计算坐标，也不能只显示处理结果而不录像。建议同时保存原始画面或至少保存包含时间戳、目标位置、检测状态的处理后画面。

### 2.2 摄像头安装与画面要求（说明第 6 项）

- 摄像头可固定在摆杆凹槽上方。
- 回传画面必须覆盖整个摆杆。
- 必须完整、清晰地看到钢球滚动轨迹，并能够判定钢球位置。

“可固定在凹槽上方”是推荐方案，不是说随便斜拍也等价。顶视、固定焦距、刚性连接是最容易做好标定和抗干扰的安装方式。

### 2.3 检测方式要求（说明第 7 项）

- 凹槽内钢球位置检测必须采用摄像头。
- 摆杆长 25 cm，由 4 分 PPR 水管改造而成。
- 水管外径约 2 cm、壁厚 0.34 cm，凹槽内钢球直径约 1 cm。
- 刻度贴在凹槽剖面边沿，不能贴在凹槽内，刻度间距为 0.1 cm。
- 摆杆内壁必须保持光滑原状，不能通过凹坑或增摩擦改造帮助控制。
- 整个摆杆不能超出车身。

这里的“必须采用摄像头”意味着不能用红外对管、激光测距、电阻膜、磁编码等传感器替代钢球位置视觉检测。

### 2.4 视觉测量需要支撑的控制指标

| 测试项 | 视觉需要完成的工作 | 关键指标 |
| --- | --- | --- |
| 静止小车，O → +5 cm → -5 cm | 连续测量钢球位置，供控制器换向和稳定 | 总时间 ≤ 5 s；±5 cm 处最大绝对误差 ≤ 1 cm |
| A 到 B 行驶 | 在车辆加速、振动和转弯前保持 O 点闭环 | AB 时间 ≤ 8 s；钢球误差绝对值 ≤ 1 cm |
| 整圈中心点控制 | 全程连续检测，不得在弯道或振动时丢球 | 总时间 ≤ 30 s；钢球误差绝对值 ≤ 1 cm |
| 整圈任意指定位置 | 接收任意目标坐标，而不是只识别 O、±5 cm | 总时间 ≤ 30 s；钢球误差绝对值 ≤ 1 cm |

因此视觉算法必须支持整个约 `[-12 cm, +12 cm]` 的有效范围，不能只针对三个固定位置写判断逻辑。

### 2.5 按题目1～6项逐项拆解视觉职责

先给出总览：

| 题目项 | 视觉是否参与 | 视觉的主要职责 | 视觉明确不负责的内容 |
| --- | --- | --- | --- |
| 1. 图传、录像与回放 | 直接负责 | 实时画面、稳定传输、完整录像、可靠回放 | 不允许图传阻塞钢球控制链路 |
| 2. 小车循迹一圈并停车 | 只承担图传记录 | 继续拍摄钢球并记录本次测试 | 黑线循迹、A点识别、停车控制和计时不能依赖摄像头 |
| 3. 静止车，O→+5→-5 cm | 直接参与闭环 | 连续输出钢球位置和速度，支撑换向与稳定 | 不在视觉模块中写死运动顺序或直接产生步进脉冲 |
| 4. A到B行驶，钢球稳定在O | 直接参与闭环 | 在加速、振动下持续输出O点相对位置 | 小车循迹与车辆加速度前馈不由钢球摄像头负责 |
| 5. 整圈行驶，钢球稳定在O | 直接参与闭环 | 整圈不丢球、低延迟输出并完整录像 | 不负责判断黑线和规划小车速度 |
| 6. 整圈行驶，钢球稳定在任意位置 | 直接参与闭环 | 全量程测量任意位置，显示目标和误差带 | 不能只识别O、±5 cm三个固定点 |

#### 2.5.1 第1项：安装图传装置、实时显示、完整录像和回放（6分）

题目要求的重点不是“有一个摄像头”，而是形成一套可实际验收的图传系统。

视觉系统必须完成：

1. 图传发送模块和摄像头稳固安装在小车上。
2. 摄像头完整覆盖摆杆、凹槽和钢球全行程。
3. 小车运动、振动和转弯时，画面不能明显抖动、失焦或频繁曝光跳变。
4. 将实时画面发送到环形线路以外的接收、显示和存储设备。
5. 每次正式测试从开始到结束形成一个完整视频文件。
6. 测试结束后正确关闭文件，能够按要求立即打开回放。
7. 回放中能够清楚判断钢球在摆杆上的位置和运动过程。

建议回传画面叠加：

```text
测试模式
录像时间戳
钢球中心标记
当前位置 x_cm
目标位置 target_cm
位置误差 error_cm
检测置信度
检测/预测/丢球状态
实际FPS和测量延迟
```

录像文件建议按一次测试一个文件保存：

```text
test_01_video_时间.mp4
test_02_lap_时间.mp4
test_03_static_move_时间.mp4
test_04_A_to_B_时间.mp4
test_05_center_lap_时间.mp4
test_06_any_target_lap_时间.mp4
```

这一项还隐含两个工程要求：

- 图传网络变慢或接收端断开时，不能拖慢车载钢球检测和摆杆控制。
- 录像编码和文件写入不能占满视觉控制线程，应该使用独立线程和小容量队列，只保留最新待显示帧。

验收前必须实际演练：启动测试、实时观看、停止录像、找到文件、完整回放，不能只验证“网页能打开”。

#### 2.5.2 第2项：小车循迹一圈并停到A点（16分）

这一项的主要任务是小车循迹、计时和停车，不是钢球视觉控制任务。

按照题目限制：

```text
黑线检测 -> 红外光电模块
小车转向和速度 -> 小车运动控制器
A点停车判断 -> 红外/里程/任务逻辑
总时间显示 -> 主控制器计时
```

摄像头不能被用来识别黑线、A点或替代红外循迹模块。

视觉在这一项仍需要做的工作是：

1. 图传装置保持开启，验证小车运动时画面仍然稳定。
2. 按第1项要求记录本次测试过程。
3. 如果钢球安装在凹槽上，持续拍摄其运动，作为车体振动和安装可靠性的证据。
4. 可以接收主控制器提供的测试状态和计时值，将其叠加到回传画面，但计时源不能来自视频帧计数。

视觉不应该输出：

```text
左转/右转命令
黑线横向误差
A点停车命令
小车电机PWM
```

这一项对视觉的实际价值是验证机械安装和图传链路：如果小车跑一圈后摄像头位置发生变化，后续钢球厘米标定将全部失效。

#### 2.5.3 第3项：静止小车，钢球O→+5 cm→-5 cm（13分）

这一项是视觉位置闭环的基础验收项目。

任务顺序应由控制器或任务状态机管理：

```text
目标0：O点
    ↓
目标1：+5 cm
    → 到达并满足稳定条件
目标2：-5 cm
    → 到达并满足稳定条件
结束计时
```

视觉必须持续完成：

1. 检测钢球中心像素 `(u, v)`。
2. 将像素位置映射为相对于O点的 `x_cm`。
3. 滤波估计钢球相对速度 `velocity_cm_s`。
4. 输出采集时间戳、检测置信度和丢球状态。
5. 在钢球快速越过O点和目标点附近慢速稳定时都能正确检测。
6. 在 `+5 cm` 和 `-5 cm` 两侧保持一致的坐标精度，不能只对一侧调参。
7. 录像中显示目标点、当前位置和允许误差带。

推荐视觉状态输出：

```text
BallState {
    timestamp
    position_cm
    velocity_cm_s
    confidence
    detected
    predicted
}
```

控制器根据视觉状态计算：

```text
error_cm = target_cm - position_cm
```

然后由PID/状态反馈计算摆杆目标角度，再转换成步进电机目标步数和脉冲轨迹。视觉模块不应该直接发送步进电机STEP脉冲。

题目允许的最终位置最大误差是 `±1 cm`，但不能把全部误差预算都留给视觉。建议静态视觉测量误差尽量达到：

```text
O、±5 cm处平均误差 ≤ 0.2 cm
最大测量误差 ≤ 0.3 cm
```

这样才能给控制误差、机械回差、步进电机响应和车体振动留下余量。

“到达目标”不能只依据一帧图像，推荐控制器使用持续稳定条件，例如：

```text
abs(error_cm) ≤ 允许误差
abs(velocity_cm_s) ≤ 速度阈值
连续保持一定时间
```

#### 2.5.4 第4项：A到B行驶，钢球稳定在O点（20分）

这一项在第3项静态闭环的基础上增加了小车加速、振动和线路运动扰动。

视觉必须完成：

1. 小车从A启动加速时，连续输出钢球相对O点的位置。
2. 车体振动造成图像轻微位移和模糊时仍能检测钢球。
3. 摆杆产生控制角度后，仍能保持正确的像素/厘米映射。
4. 不因为图传编码或网络延迟而积压旧帧。
5. A到B整个区间持续记录位置、误差、置信度和丢球状态。
6. 回传画面显示 `target=0 cm` 和 `±1 cm` 允许区间。

这时摆球控制链路为：

```text
摄像头 -> 钢球相对位置和速度 -> PID反馈角

小车控制器/编码器/IMU -> 小车纵向加速度 -> 前馈补偿角

PID反馈角 + 加速度前馈角
    -> 摆杆目标角
    -> 步进电机目标步数和STEP/DIR
```

必须明确：加速度前馈数据一般来自IMU、编码器或小车运动规划，不是由钢球摄像头直接测量。视觉负责闭环修正前馈模型和实际机构之间的剩余误差。

若没有加速度前馈，视觉反馈仍然可以控制钢球，但必须等钢球先偏离O点后才能纠正，A点启动阶段的最大误差可能更大。

题目同时规定AB时间 `≈8 s`，但AB计时和B点事件应由小车主控制器处理。视觉可以记录、显示这些事件，不应依赖图像识别B点。

#### 2.5.5 第5项：整圈行驶，钢球稳定在O点（20分）

这一项不是简单地把第4项运行时间延长，而是增加整圈持续可靠性要求。

视觉必须完成：

1. 整圈持续检测钢球，不能只在直线段有效。
2. 在启动、加速、减速、转弯和通过接缝振动时保持低延迟输出。
3. 固定曝光、白平衡、增益和焦距，避免环境光变化造成检测参数漂移。
4. 跟踪算法使用真实时间戳，短时模糊可有限预测，但不能长时间输出虚假的预测位置。
5. 检测失败时立即降低置信度，超过时限后通知控制器进入安全策略。
6. 录像覆盖从按键启动到完成一圈通过A点的全过程。
7. 回放中能够检查整圈任意时刻的钢球位置误差。

推荐记录以下时序数据，便于赛后将视频和控制曲线对齐：

```text
timestamp
frame_sequence
position_cm
velocity_cm_s
target_cm=0
error_cm
confidence
detected/predicted
car_acceleration
rod_target_angle
stepper_target_steps
```

整圈任务最怕的不是单帧少量噪声，而是：

- 摄像头支架在振动后发生位移，使原标定整体偏移。
- 图传或录像阻塞检测线程，控制器持续收到旧位置。
- 自动曝光跳变造成钢球短时消失。
- 跟踪器丢球后长期预测，却没有通知控制器。
- 步进电机失步后真实摆杆角和软件估计角不一致。

因此这一项必须同时验证视觉精度、延迟、机械固定、通信和失效保护。

#### 2.5.6 第6项：整圈行驶，钢球稳定在任意指定位置（20分）

这一项要求系统支持连续坐标目标，而不是三个离散状态。

视觉算法必须满足：

1. 在摆杆有效范围内输出连续 `x_cm`，不能只分类为O、+5和-5。
2. 标定样本覆盖中心和两端，至少包含 `-10、-5、0、+5、+10 cm`。
3. 在全范围验证映射误差，尤其检查靠近两端的透视误差和视差。
4. 钢球位于任意静止位置时仍能检测，不能只依赖会吸收静止目标的自适应背景模型。
5. 目标改变时不需要切换视觉检测参数或重新标定。
6. 接收控制器的 `target_cm` 仅用于画面叠加；视觉检测本身始终输出钢球实际位置。
7. 回传画面显示任意目标线和 `target_cm±1 cm` 误差带。

正确的软件关系为：

```text
用户/评测指定 target_cm
             ↓
      控制器任务状态机
             ↓
error_cm = target_cm - vision.position_cm
             ↓
       摆杆位置控制
```

视觉模块的关系为：

```text
相机图像 -> 钢球检测 -> 全量程厘米坐标 position_cm
```

视觉不应写成：

```text
if 靠近O: 输出0
elif 靠近右侧: 输出+5
else: 输出-5
```

这种离散判断即使能完成第3项，也无法完成第6项。

任意位置任务还要求标定和相机视场保留足够余量。摄像头应覆盖约 `[-12 cm,+12 cm]` 的整个可用行程，钢球在两端和摆杆最大控制角时都不能出画。

### 2.6 六项任务对应的视觉验收清单

正式联调时，可以按下面的顺序检查：

#### 第1项验收

- [ ] 发送模块和摄像头在小车上固定可靠。
- [ ] 接收、显示和存储设备完全位于线路外。
- [ ] 画面覆盖完整摆杆和钢球全行程。
- [ ] 能实时显示、开始录像、结束录像并完整回放。
- [ ] 接收端断线不会阻塞车载钢球控制。

#### 第2项验收

- [ ] 黑线循迹只使用红外光电模块。
- [ ] 摄像头不输出转向、黑线误差和停车命令。
- [ ] 小车跑一圈后摄像头支架和标定没有发生位移。
- [ ] 本次测试录像完整。

#### 第3项验收

- [ ] O、+5、-5 cm的正负方向定义正确。
- [ ] 快速运动和目标点静止时均能检测钢球。
- [ ] 视觉连续输出位置、速度、置信度和时间戳。
- [ ] ±5 cm附近视觉测量误差明显小于题目允许的1 cm。
- [ ] 录像能看出到达、折返和最终稳定过程。

#### 第4项验收

- [ ] A点启动加速时没有长时间丢球。
- [ ] PID视觉反馈和车辆加速度前馈的数据来源分离。
- [ ] AB计时和B点事件由主控制器提供，不依赖摄像头循迹。
- [ ] AB全过程位置误差和录像可检查。

#### 第5项验收

- [ ] 整圈所有路段持续检测，不积压旧帧。
- [ ] 振动、转弯和光照变化下仍能工作。
- [ ] 丢球和过期数据能够触发控制安全策略。
- [ ] 整圈视频和状态数据时间戳一致。

#### 第6项验收

- [ ] 支持全量程连续坐标，不是三点分类器。
- [ ] 任意目标只改变控制器的 `target_cm`。
- [ ] 两端映射误差、静止球检测和最大摆角视场均已验证。
- [ ] 画面显示任意目标线和±1 cm允许误差带。

## 3. 视觉系统应如何划分

推荐的数据链路如下：

```text
车载摄像头
   │
   ├─> 采集与时间戳
   │      │
   │      ├─> 摆杆 ROI / 几何校正
   │      ├─> 钢球检测
   │      ├─> 像素坐标转厘米坐标
   │      └─> 滤波、速度估计、置信度判断
   │                    │
   │                    └─> 摆杆控制器（高优先级、低延迟）
   │
   └─> 画面叠加标注 -> 编码 -> 无线图传 -> 场外显示与录像

红外循迹阵列 -> 小车运动控制器（独立链路，不使用摄像头）
```

控制链路的实时性应高于网页推流和录像。JPEG 编码、网络阻塞、客户端断线都不应拖慢钢球检测与摆杆控制。

## 4. 推荐硬件与安装方式

### 4.1 摄像头位置

首选将摄像头安装在摆杆正上方，使光轴尽量垂直摆杆顶面，并让 25 cm 摆杆横向占据画面的大部分宽度。

建议：

- 摄像头和摆杆支撑结构刚性连接，避免相对位姿随车体振动变化。
- 使用固定焦距、固定曝光、固定白平衡和固定增益，防止自动参数跳变影响检测。
- 画面必须保留摆杆两端、中心 O 点和一部分刻度，便于现场直接判读。
- 避免视场过大。摆杆在画面中越小，每厘米对应的像素越少，定位越不稳定。
- 加遮光罩或使用柔和、均匀的补光，重点抑制钢球高光、环境灯闪烁和水管反光。
- 补光结构不能遮挡钢球，也不能构成对凹槽内壁的增摩擦改造。

### 4.2 分辨率和帧率

当前工程统一请求 `640×480 @ 120 fps`，优先利用高帧率降低采样周期，而不是盲目追求 1080p。

若 25 cm 摆杆在画面内占 500 像素，则：

```text
比例约为 500 px / 25 cm = 20 px/cm
1 cm 误差对应约 20 px
0.1 cm 刻度对应约 2 px
```

这对题目规定的 ±1 cm 控制误差已有足够的测量余量。更关键的是低延迟、稳定帧率和不积压旧帧。摄像头目标帧率统一为 120 FPS；视觉算法应尽量跟上输入，并始终只处理最新帧。端到端测量延迟尽量控制在 30 ms 内。启动日志必须核对硬件实际接受的 FPS，不能只相信设置值。

## 5. 标定：如何从像素得到厘米

### 5.1 坐标定义

按题图定义摆杆中心点为 O，沿摆杆向右为正方向：

```text
x_cm = 0       表示钢球在中心 O
x_cm = +5      表示钢球在 O 点右侧 5 cm
x_cm = -5      表示钢球在 O 点左侧 5 cm
```

控制软件内部必须统一正负方向，若舵机/电机方向相反，应在执行机构层处理，不要让视觉坐标定义随控制代码改变。

### 5.2 简单标定法

摄像头接近正俯视、畸变较小时，可记录中心点像素 `u0`，以及两个已知刻度点 `(u1, x1)`、`(u2, x2)`：

```text
scale = (x2 - x1) / (u2 - u1)      # cm/px
x_cm  = x1 + (u_ball - u1) * scale
```

建议使用相距尽量远的两个刻度点，例如 `-10 cm` 和 `+10 cm`，可降低点选误差。

### 5.3 更稳妥的标定法

若摄像头存在斜视或广角畸变，应执行：

1. 相机内参和畸变标定。
2. 用摆杆边缘/固定标志点计算透视变换。
3. 将摆杆区域矫正为固定大小的俯视图。
4. 在矫正图上做一维坐标换算。

摄像头与摆杆在车上的相对位置固定，因此标定参数可保存到配置文件，启动时加载。每次装配后仍应检查 O、±5 cm 和两端点的实测误差。

## 6. 钢球检测算法建议

钢球是反光物体，它的亮度和颜色会随环境变化，所以不建议只写一个固定灰度阈值。推荐采用“固定 ROI + 多特征候选 + 时序约束”的传统视觉方案，计算量小，也容易在比赛现场调试。

### 6.1 处理流程

1. 读取最新帧并记录采集时间戳。
2. 畸变校正，并裁剪固定的摆杆 ROI。
3. 对 ROI 做灰度化、轻度滤波或亮度归一化。
4. 使用背景差分、边缘/圆形特征或亮暗组合产生钢球候选区域。
5. 按尺寸、圆度、所在凹槽范围和与上一帧的位置距离筛选候选。
6. 求钢球中心亚像素或像素坐标 `(u_ball, v_ball)`。
7. 将 `u_ball` 换算为 `x_cm`。
8. 使用 α-β 滤波或卡尔曼滤波得到平滑位置和速度。
9. 输出检测置信度；丢球时进入安全策略，不能把错误坐标送给控制器。

### 6.2 候选检测的优先顺序

推荐先测试以下组合：

- **背景差分**：摄像头和摆杆相对固定，空槽背景容易预先采集；对运动钢球有效。
- **轮廓几何筛选**：钢球投影尺寸基本固定，可按面积、宽高比、圆度过滤刻度和槽边缘。
- **霍夫圆辅助**：可作为候选确认，不建议单独依赖，因为反光和遮挡可能让圆边缘不完整。
- **时序跟踪**：限制相邻帧最大位移，用预测位置选择正确候选，可显著降低高光误检。

若钢球停稳后背景差分逐渐失效，必须保留基于外观/边缘的静态检测分支，或使用固定参考背景直接作绝对差分，而不是会把静止钢球吸收进去的自适应背景模型。

### 6.3 为什么不优先上神经网络

目标只有一个、运动区域固定、几何尺寸固定，传统 OpenCV 通常更低延迟、更易标定，也不需要收集和标注大量数据。只有在现场反光、遮挡和背景变化使传统方法长期不稳定时，才值得使用小型检测/分割模型；即便使用模型，坐标标定、滤波和失效保护仍然不可省略。

## 7. 滤波、延迟和闭环控制接口

### 7.1 不要过度平滑

移动平均虽然简单，但窗口越长，控制延迟越大，容易造成钢球来回振荡。推荐 α-β 滤波或常速度卡尔曼滤波，同时估计：

```text
state = [position_cm, velocity_cm_s]
```

滤波必须使用真实帧时间戳计算 `dt`，不要假设每帧间隔固定。

### 7.2 建议接口

视觉线程向控制线程发布最新状态，而不是排队处理所有旧帧：

```python
BallState(
    timestamp=...,       # 单调时钟
    position_cm=...,     # 相对 O 点的坐标
    velocity_cm_s=...,
    confidence=...,      # 0.0 ~ 1.0
    detected=True,
)
```

控制器应检查状态新鲜度。例如超过 100 ms 没有可信的新测量，应限制摆杆动作或进入预设安全姿态，不能继续使用陈旧坐标大幅控制。

### 7.3 视觉与控制的职责边界

- 视觉负责输出带置信度的位置、速度和时间戳。
- 控制器负责目标值、误差计算、PID/状态反馈以及执行机构限幅。
- “任意指定位置”应作为控制器的 `target_cm` 参数输入，视觉算法本身无需为不同目标切换检测模式。

## 8. 图传和录像实现

建议画面至少叠加以下内容，方便评审和赛后定位问题：

- 当前钢球中心标记。
- O 点、目标点和允许误差带（目标 ±1 cm）。
- 当前 `x_cm`、目标 `target_cm`、误差、检测置信度。
- 帧率、测量延迟和录像时间戳。
- 检测丢失时醒目的 `BALL LOST` 状态。

录像要点：

- 按下启动键时同步开始一段新录像，测试结束后正常关闭文件，避免断电导致文件损坏。
- 文件名包含测试模式和时间，例如 `center_lap_20260729_103000.mp4`。
- 正式测试前在接收设备上实际打开录像回放，检查帧率、完整性和标注可读性。
- 无线图传断线不应中断车载视觉控制。建议车载端也保留本地录像作为备份，但仍需满足题目要求的场外实时显示和存储。
- 网络推流只发送“最新帧”，网络慢时主动丢弃旧画面，避免延迟不断累积。

## 9. 当前工程能力与缺口

当前工程中的 `xbhdcc_camera/xbhdcc_tools.py` 已具备：

- Linux/Windows 摄像头枚举和 OpenCV 采集辅助。
- 两路 MJPEG 网页视频流。
- 对帧数据加锁，支持原图和处理图并排预览。

`xbhdcc_camera/test.py` 当前只是打开编号为 9 的摄像头，并把同一画面推送到两个通道。

当前还缺少比赛所需的关键功能：

- 摄像头参数配置和可靠的设备选择，不能把设备号 `9` 写死。
- 摆杆 ROI 与相机标定参数的保存/加载。
- 钢球检测、像素到厘米换算、滤波与置信度。
- 视觉状态到摆杆控制器的低延迟通信接口。
- 录像、回放验证和按键触发的测试文件管理。
- 控制线程与 MJPEG 推流线程的解耦及性能监测。
- 丢球、过期帧、摄像头断开和网络断开等异常处理。

现有双路推流很适合调试时定义为：

```text
通道 0：原始相机画面
通道 1：矫正后的 ROI + 钢球检测/坐标/目标点叠加画面
```

但现有网页 MJPEG 只解决“实时查看”的一部分，不能视为已经完成题目第 1 项，因为它还没有录像和可靠回放功能。

## 10. `project1` 工程中可借鉴的内容

已检查参考工程：`/home/sunrise/rdk_linux_dian_sai/project1`。该工程原本解决的是矩形靶板识别、激光点映射和云台串口控制，不是本题的车载钢球检测。因此应复用其通用基础设施和工程组织方式，不应直接复制其目标识别参数和坐标模型。

### 10.1 复用价值总览

| 参考模块 | 复用等级 | 可借鉴内容 | 本题需要的改造 |
| --- | --- | --- | --- |
| `Test/CameraTest_Cali_qipan.py` | 高 | 相机模式校验、棋盘格标定、畸变矫正映射缓存、图片采集和视频录像 | 对本题实际摄像头重新标定；拆出正式的相机标定模块 |
| `Threads/laser_track_Kalman.py` | 高 | 采集、识别、通信、显示分线程；满队列丢旧帧；短时丢失预测 | 检测对象换为钢球；增加时间戳、厘米坐标、置信度及过期保护 |
| `Algorithm/CenterKalman.py` | 中高 | 恒速卡尔曼模型、动态 `dt`、短时纯预测和复位 | 从二维像素改为一维厘米状态；输出速度和浮点结果；按真实时间更新 `dt` |
| `Drivers/my_serial.py` | 中 | 帧头、浮点数据和校验和组成的二进制串口包 | 协议改为钢球状态；加入消息类型、序号、检测状态、时间戳/新鲜度和更强校验 |
| `Drivers/camera.py` | 中 | V4L2 + MJPG、高帧率采集、打开/释放封装、读取实际参数 | 设备路径、旋转、分辨率和 FPS 配置化；设置缓冲深度；固定曝光等参数 |
| `My_UI/app.py` | 中 | 原图/阈值图/边缘图/结果图四视图、滑块调参、JSON 保存、后台线程 | 改成钢球检测调试面板；正式运行时关闭重型 UI，避免影响控制实时性 |
| `Algorithm/CircleGet.py` | 原理高、代码中 | 平面单应性、物理坐标与像素坐标的透视映射公式可以复用 | 交换到本题所需的映射方向，重写摆杆标定点、尺寸和 `x_cm` 输出；不沿用 A4 圆轨迹逻辑 |
| `Algorithm/CenterGet.py` | 低 | 灰度、模糊、形态学、轮廓筛选的基本 OpenCV 流程 | 原算法专门寻找四边形靶框，不能直接检测圆形反光钢球 |

### 10.2 最值得迁移：相机标定与畸变矫正

参考文件：[CameraTest_Cali_qipan.py](/home/sunrise/rdk_linux_dian_sai/project1/Test/CameraTest_Cali_qipan.py)。

其中值得直接借鉴的工程做法有：

- 用 `MJPG` 打开 V4L2 摄像头，并设置 `CAP_PROP_BUFFERSIZE = 1`，降低读取到旧帧的概率。
- 设置分辨率和帧率后，重新读取实际 FOURCC、宽高和 FPS；硬件没有接受目标模式时立即报错，而不是假定设置成功。
- 从 `.npz` 加载相机矩阵、畸变参数和标定分辨率。
- 使用 `initUndistortRectifyMap()` 预计算映射表，逐帧只执行 `remap()`，比每帧重新计算映射更合适。
- 检查当前画面和标定画面的宽高比，避免误用标定结果。
- 已有棋盘格采样、标定、图片保存和 `VideoWriter` 录像流程，可拆分后接入当前工程。

参考工程现有 `camera_calibration.npz` 的信息是：

```text
标定分辨率：640×480
棋盘格内角点：9×6
格长：23 mm
平均重投影误差：约 0.433 px
```

这个重投影误差本身不错，但 **该文件只能对应生成它的那一颗摄像头、镜头状态和成像分辨率**。如果本题更换摄像头、重新调焦、改变镜头或宽高比，必须重新标定，不能直接把旧 `.npz` 当作通用参数。

迁移建议：

```text
project1/Test/CameraTest_Cali_qipan.py
        ├─> 提取 CameraConfig / open_camera
        ├─> 提取 CameraCalibrator（离线标定工具）
        ├─> 提取 Undistorter（正式运行使用）
        └─> 提取 VideoRecorder（录像线程使用）
```

相机内参标定只能解决镜头畸变。钢球像素到摆杆厘米坐标仍需另做“摆杆平面/刻度标定”，二者不能混为一件事。

### 10.3 最值得迁移：只保留最新帧的多线程架构

参考文件：[laser_track_Kalman.py](/home/sunrise/rdk_linux_dian_sai/project1/Threads/laser_track_Kalman.py)。

其架构已经把==相机采集、视觉算法、串口发送和显示分开==。==采集线程发现容量很小的队列已满时，会先取走旧帧再放入新帧==。这一点非常适合本题，因为平衡控制最怕“每一帧都处理，但处理的是越来越旧的画面”。

建议在当前工程中采用类似结构：

```text
CaptureThread  --最新帧-->  BallVisionThread  --最新状态-->  Control/SerialThread
      │                          │
      └--最新原图-->             └--最新标注图-->  Stream/RecordThread
```

但需要进一步改进：

- 队列元素应携带采集时刻，使用 `time.monotonic()`，而不只是传图像。
- 算法输出应是 `BallState`，而不是无语义的 `(center, laser_pt, distance)` 元组。
- 发送前检查状态年龄和置信度，禁止把过期结果继续送入闭环。
- 显示和录像必须消费图像副本或只读图像，不能阻塞控制状态发布。
- 线程退出应使用 `threading.Event`，并在关闭时正确 `join()`、释放摄像头和录像文件。
- 不要在 50～100 Hz 循环中逐帧 `print()`，否则终端 I/O 会造成抖动；应限频输出统计信息。

### 10.4 可改造：卡尔曼滤波与短时丢球策略

参考文件：[CenterKalman.py](/home/sunrise/rdk_linux_dian_sai/project1/Algorithm/CenterKalman.py)。

该模块采用状态 `[px, py, vx, vy]` 的恒速模型，提供：

- 有测量时 `predict + correct`。
- 无测量时 `predict_only()`。
- `update_dt()` 动态修改帧间隔。
- 跟丢过久后 `reset()`。

这些思想适合本题，但建议重写为一维状态：

```text
状态：[x_cm, vx_cm_s]
测量：[x_cm]
时间步长：相邻采集时间戳之差
输出：浮点位置、浮点速度、是否为纯预测
```

原实现最终把结果四舍五入为整数像素，会丢失不必要的精度；本题应一直保留浮点厘米值，到画图时才转整数像素。

还要注意，参考线程最多允许 15 帧纯预测。若按当前 120 FPS 计算，这大约是 125 ms，仍可能足以让钢球发生明显运动。对平衡闭环而言，预测结果只能跨越极短遮挡，建议按“毫秒”而非“帧数”设限，并同时降低置信度。例如：

```text
丢失 0～50 ms：允许短时预测，控制动作限幅
丢失 50～100 ms：不再把预测当作可靠测量，控制器进入保守模式
丢失 >100 ms：状态无效，执行明确的安全策略并重新捕获
```

最终阈值必须依据实车动力学和测试数据确定，上述时间只适合作为初始值。

### 10.5 可改造：串口二进制通信

参考文件：[my_serial.py](/home/sunrise/rdk_linux_dian_sai/project1/Drivers/my_serial.py)。

现有协议使用 `0xAA 0x55` 帧头、两个小端 `float` 和 1 字节累加校验，结构简单，适合快速联调。可借鉴 `struct.pack()` 和独立串口线程，但不建议原样作为本题最终协议，因为它只表达两个像素偏差，且没有消息类型、序号、有效标志和时间信息。

本题可以定义类似状态包：

```text
帧头 | 版本/类型 | 序号 | x_cm | vx_cm_s | confidence | flags | CRC16
```

其中 `flags` 至少应表示：检测有效、当前值是否为预测、标定是否加载。接收控制器还应根据序号或接收超时识别通信中断。函数名 `send_deta` 也是旧工程遗留拼写，迁移时应改成含义明确的 `send_ball_state`。

如果视觉和摆杆控制都运行在同一块 RDK 板上，优先采用进程内最新状态或共享内存，串口只用于连接独立 MCU，不需要为了复用旧代码而强行增加串口链路。

### 10.6 可借鉴但不要直接作为正式算法：调参界面

参考文件：[app.py](/home/sunrise/rdk_linux_dian_sai/project1/My_UI/app.py)。

该界面同时显示原图、二值图、边缘图和最终结果，并用滑块调整阈值、Canny、面积和模糊参数。这种可视化方式很适合快速采集钢球在不同光照和位置下的数据，并判断误检来自哪一个处理阶段。

建议借鉴其“四视图 + 参数滑块 + JSON 保存”交互，但做以下调整：

- 参数保存到 JSON/YAML 配置，不要像旧代码那样通过字符串匹配直接重写算法源文件。
- 增加 ROI、钢球最小/最大直径、圆度、背景差阈值和最大帧间位移等参数。
- 增加实时曲线或日志：原始 `x_cm`、滤波 `x_cm`、速度、置信度和端到端延迟。
- 调参 UI 仅用于开发与标定；正式比赛进程使用已保存参数并保持轻量。

### 10.7 可以复用的基本原理：标定、映射与透视变换

参考文件：[CircleGet.py](/home/sunrise/rdk_linux_dian_sai/project1/Algorithm/CircleGet.py)。

参考工程中的相机标定、==坐标映射和透视变换==不仅是“思路上类似”，其数学原理和 OpenCV API 都可以复用。不能直接复用的是旧相机生成的参数、A4 纸物理模型、坐标方向以及圆轨迹业务代码。

#### 10.7.1 相机内参标定与检测目标无关

棋盘格标定解决的是镜头成像问题，与后续检测矩形、激光点还是钢球无关。参考工程中的流程可以继续使用：

```text
采集多角度棋盘格图像
    -> 提取并细化角点
    -> calibrateCamera()
    -> 得到 camera_matrix 和 dist_coeffs
    -> initUndistortRectifyMap()
    -> 每帧 remap() 去畸变
```

因此可以复用标定程序和 `Undistorter` 的实现方式，但需要用本题实际使用的摄像头、镜头焦距和分辨率重新生成参数。旧 `.npz` 文件不是通用公式，而是某一套具体硬件的测量结果。

#### 10.7.2 单应性公式相同，主要区别是==映射方向==

平面透视映射使用 3×3 单应矩阵。若定义==从图像到摆杆物理平面==的变换，则有：

```text
                  [u]
s [X, Y, 1]^T = H [v]
                  [1]
```

其中：

- `(u, v)` 是图像中的像素坐标。
- `(X, Y)` 是摆杆平面中的物理坐标，可以直接使用 cm。
- `s` 是齐次坐标的比例因子。
- `H` 是由至少四对不共线对应点求出的单应矩阵。

`CircleGet.py` 当前使用：

```python
H = cv2.getPerspectiveTransform(physical_corners, image_corners)
```

它计算的是“物理坐标 -> 图像坐标”，目的是把 A4 纸上的理论圆周点投影到画面。H 题主要需要“==图像坐标 -> 摆杆厘米坐标==”，因此交换对应点的输入方向即可：

```python
image_points = np.float32([
    [u_left_top,     v_left_top],
    [u_right_top,    v_right_top],
    [u_right_bottom, v_right_bottom],
    [u_left_bottom,  v_left_bottom],
])

# 以摆杆中心 O 为原点，摆杆长 25 cm、外宽约 2 cm。
physical_points = np.float32([
    [-12.5, -1.0],
    [ 12.5, -1.0],
    [ 12.5,  1.0],
    [-12.5,  1.0],
])

H_image_to_rod = cv2.getPerspectiveTransform(
    image_points, physical_points
)
```

得到钢球像素中心后，可直接换算为厘米坐标：

```python
ball_pixel = np.array([[[u_ball, v_ball]]], dtype=np.float32)
ball_xy_cm = cv2.perspectiveTransform(
    ball_pixel, H_image_to_rod
)[0, 0]

x_cm = float(ball_xy_cm[0])
```

这里的 `x_cm` 就是相对于中心 O 的位置，可直接作为滤波器和控制器的输入。实际标定时，四个物理点应以作品上真实测量的摆杆有效边界为准，不应只按题面名义尺寸假定。

如果已经存在物理到图像的矩阵 `H_physical_to_image`，理论上也可求逆：

```text
H_image_to_physical = inverse(H_physical_to_image)
```

但工程上直接按照目标方向调用 `getPerspectiveTransform()` 更清楚，也更不容易混淆坐标含义。

#### 10.7.3 根据安装条件选择映射复杂度

完整单应性不是任何情况下都必须使用。应选择能够满足全行程误差指标的最简单模型。

==**正俯视且畸变已校正：一维线性映射**==

记录两个相距较远的已知刻度 `(u1, x1)`、`(u2, x2)`：

```text
x_cm = x1 + (u_ball - u1) * (x2 - x1) / (u2 - u1)
```

这种方法参数少、运行快、易于检查。建议再使用 O、±5 cm、±10 cm 等未参与拟合的点验证残差。

==**存在轻微斜视：一维射影映射或多点拟合**==

沿摆杆方向的透视关系可写为：

```text
x_cm = (a*u_ball + b) / (c*u_ball + 1)
```

使用三个及以上已知刻度点拟合参数，并用剩余刻度验证。若误差主要来自轻微非线性，也可使用低阶多项式，但应避免高阶拟合在端点发散。

**斜拍明显或摆杆在画面中倾斜：二维单应性**

用摆杆四个不共线边界点建立图像到物理平面的单应矩阵，或先 `warpPerspective()` 得到固定尺寸的标准俯视 ROI，再在 ROI 内检测和换算坐标。

推荐判断顺序：

```text
先做相机去畸变
    -> 测试一维线性标定的全量程残差
    -> 不满足精度时尝试一维射影/多点拟合
    -> 仍不满足或安装明显倾斜时使用二维单应性
```

不要只比较画面是否“看起来变正”，应使用多个真实刻度点统计最大绝对误差和均方误差。

#### 10.7.4 钢球高度会带来非平面视差

单应性严格适用于同一物理平面上的点。摆杆边沿、刻度和钢球图像中心并不一定处于完全相同的高度；钢球直径约 1 cm，其中心高于槽底。如果摄像头明显斜拍，使用摆杆边界平面求出的单应矩阵去换算钢球中心，可能产生系统性视差：

```text
摄像头越斜、安装距离越近、钢球与标定平面高度差越大
    -> 沿摆杆方向的位置偏差越明显
```

处理优先级如下：

1. 优先让摄像头接近正俯视，从机械上减小视差。
2. 用钢球实际放置在 O、±5 cm、±10 cm 等位置时的像素中心做坐标标定，而不是只用摆杆边沿。
3. 若仍有稳定的系统误差，使用多点一维射影/多项式拟合修正。
4. 每次改变相机高度、角度、焦距或支架位置后重新验证标定。

#### 10.7.5 `CircleGet` 中哪些保留，哪些重写

可以保留或借鉴：

- `getPerspectiveTransform()` 建立单应矩阵的方法。
- 齐次坐标归一化和 `perspectiveTransform()` 的使用方式。
- 四个对应点必须顺序一致的原则。
- 将物理尺寸直接作为目标坐标，从而输出有实际单位的结果。

需要重写：

- A4 纸宽高、圆半径和圆周采样点。
- 原工程的角点排列函数；应为摆杆明确规定统一的左上、右上、右下、左下顺序，并编写可验证测试。
- 物理到图像的业务方向，改为本题主要使用的图像到摆杆坐标方向。
- 输出内容由圆轨迹点改为 `x_cm`、标定有效范围和映射残差。

适合本题的新模块可命名为 `RodMapper`，职责只包括加载标定、像素/厘米换算、范围检查和标定误差验证，不负责钢球检测与控制。

完整坐标链路应重新组织为：

```text
原始图像
 -> 相机内参去畸变
 -> 摆杆 ROI / 透视矫正（按需要）
 -> 钢球中心检测 (u, v)
 -> RodMapper 映射为 x_cm
 -> 一维卡尔曼滤波与速度估计
 -> 摆杆控制器
```

结论是：公式没有换，变化的是坐标定义、变换方向、物理参考点和目标对象。参考工程的数学基础值得复用，但必须针对摆杆和钢球重新建立对应点并验证厘米误差。

### 10.8 不建议直接复用：旧目标检测器和旧参数

参考文件：[CenterGet.py](/home/sunrise/rdk_linux_dian_sai/project1/Algorithm/CenterGet.py)。

`CenterGet` 的检测目标是面积较大的近似四边形：它会筛选 4 个顶点、接近 90° 的角度和边长比例，再通过对角线交点求靶心。钢球在顶视图中近似圆形，而且表面高反光，直接调用该函数大概率会检测摆杆边框、刻度或其他矩形结构，而不是钢球。

以下内容也不要直接迁移：

- `threshold_value`、Canny 阈值和 `min_area=1526` 等参数，它们来自另一套场景。
- `configs.py` 中固定的画面基准点。
- `laser_track_Kalman.py` 中由矩形面积估算距离以及距离到激光点的插值模型。
- `CircleGet.py` 中 A4 纸尺寸和圆轨迹物理参数。
- 参考工程的旧相机设备号、180° 旋转和 120 fps 假设。

这些代码可以作为 OpenCV API 示例阅读，但本题钢球检测器应基于实际采集的空槽、静止球、运动球、强反光和车体振动图像重新设计与调参。

### 10.9 建议迁移到当前工程的目录结构

不建议把 `project1` 整个目录复制进来。当前主工程统一放在 `project/` 下，可按职责选择性重构为：

```text
project/
├── Core/                 # 跨模块数据结构和公共类型
├── Driver/               # 摄像头、串口等硬件封装
├── Algorithm/            # 钢球检测、坐标映射、状态滤波
├── Services/             # 样本、录像、推流等业务服务
├── Tools/                # 标定、采样和调参入口
├── Test/                 # 自动化测试
├── Data/                 # 运行时生成的样本（忽略提交）
└── main.py               # 线程编排、启动/停止、异常处理
```

推荐迁移优先级：

1. 从标定测试脚本提取相机打开检查和 `Undistorter`。
2. 采用最新帧队列搭建采集、识别、控制、推流/录像四条独立链路。
3. 将 `CenterKalman` 改造成一维厘米状态滤波器。
4. 按本题实拍数据实现新的 `BallDetector`。
5. 最后接入串口、网页显示和录像，进行整车延迟测试。

结论：`project1` 最有价值的不是原目标识别结果，而是它已经积累的 **相机标定工具、低延迟队列模式、卡尔曼状态管理、串口封装和调参界面**。这些可以明显缩短基础设施开发时间；钢球检测、摆杆厘米标定和闭环安全策略仍必须针对 H 题重新实现。

## 11. 第一阶段已实现代码：相机与样本采集

当前工程已加入第一阶段最小代码。新增业务代码全部放在 `project/` 中，`xbhdcc_camera/` 暂时只保留原有网页双路推流代码：

```text
project/
├── Core/
│   └── models.py             # CameraConfig、FramePacket
├── Driver/
│   ├── camera.py             # 全项目唯一相机封装
│   ├── my_serial.py          # 现有串口封装
│   └── calibration/          # 已有棋盘格标定工具和参数
├── Algorithm/
│   └── KalmanFilter2D.py     # 现有参考滤波器
├── Services/
│   └── sample_storage.py     # 样本会话、元数据、图片和录像存储
├── Tools/
│   └── collect_samples.py    # 参数解析和采集流程编排
├── Test/
│   ├── test_camera.py
│   └── test_sample_storage.py
└── Data/
    └── samples/              # 运行时自动创建，不提交源码仓库
```

这里刻意只保留一个 `Camera`。新代码使用 `CameraConfig`、上下文管理器和 `capture_packet()`；旧参考代码仍可继续使用 `Camera().open()` 和 `capture()`，迁移期间不会因为封装升级立即失效。样本文件写入也集中在 `SampleSession`，采集工具不重复实现 JSON、图片命名和录像关闭逻辑。

### 11.1 `camera.py` 已完成的职责

- 支持摄像头编号或 `/dev/videoX` 路径。
- 配置 MJPG、分辨率、FPS、缓冲深度和画面旋转。
- 默认加载 `Driver/calibration/camera_calibration.npz`，对原始帧去畸变后再按安装方向旋转。
- 默认目标模式统一为 `640×480 @ 120 FPS`。
- 可选设置曝光、增益、白平衡、对焦等 UVC 参数。
- 打开后读取硬件实际接受的分辨率、FPS 和 FOURCC。
- 驱动未接受参数时给出警告，不静默假定设置成功。
- 每个 `FramePacket` 携带递增帧序号和 `time.monotonic()` 时间戳。
- 支持上下文管理，退出时保证释放摄像头。

当前 `FramePacket` 只负责采集层：

```python
@dataclass(frozen=True)
class FramePacket:
    frame: np.ndarray
    captured_at: float
    sequence: int
```

以后算法线程应直接接收这个对象，而不是只传裸 `numpy` 图像，从而可以计算处理延迟并丢弃过期状态。

### 11.2 样本目录与数据格式

每次启动采集工具会创建独立会话：

```text
project/Data/samples/20260729_123000_center/
├── session.json       # 标签、目标位置、请求参数和实际相机参数
├── samples.jsonl      # 每张图片对应的厘米标签、时间戳和帧序号
├── frame_0000_seq_00000123.jpg
├── frame_0001_seq_00000180.jpg
└── video.avi          # 仅使用 --record-video 时产生
```

默认保存的是无文字叠加的**已去畸变画面**，预览窗口上的标签和提示不会写进样本，避免干扰后续检测算法。`session.json` 会记录 `images_are_undistorted=true` 及实际使用的标定文件。只有重新采集棋盘格标定原图时才使用 `--no-undistort`。

### 11.3 实际采集命令

先查看帮助：

```bash
python3 -m project.Tools.collect_samples --help
```

采集空槽，预览窗口中按 `s` 保存，共保存 20 张后自动退出：

```bash
python3 -m project.Tools.collect_samples \
  --device /dev/video0 \
  --fps 120 \
  --label empty \
  --count 20
```

将钢球放在 O 点，采集 20 张已知 `0 cm` 样本：

```bash
python3 -m project.Tools.collect_samples \
  --device /dev/video0 \
  --label static_ball \
  --position-cm 0 \
  --count 20
```

其他标定位置只需修改 `--position-cm`，建议分别运行：

```text
-10, -5, 0, +5, +10 cm
```

例如采集 `-5 cm`：

```bash
python3 -m project.Tools.collect_samples \
  --device /dev/video0 \
  --label static_ball \
  --position-cm -5 \
  --count 20
```

采集钢球完整滚动视频，不提供固定位置标签，按 `q` 结束并正确关闭录像文件：

```bash
python3 -m project.Tools.collect_samples \
  --device /dev/video0 \
  --label rolling_ball \
  --record-video
```

在没有桌面窗口的设备上自动采集 30 张 O 点图片，每 0.2 秒一张：

```bash
python3 -m project.Tools.collect_samples \
  --device /dev/video0 \
  --label static_ball \
  --position-cm 0 \
  --headless \
  --count 30 \
  --interval 0.2
```

若摄像头倒装，使用 `--rotation 180`。曝光等参数必须先查询实际摄像头支持范围，再按驱动设置，例如：

```bash
v4l2-ctl -d /dev/video0 --list-formats-ext
v4l2-ctl -d /dev/video0 --list-ctrls-menus
```

不同 UVC 驱动对自动曝光数值的定义可能不同，不应直接照搬其他摄像头的 `--auto-exposure` 和 `--exposure` 值。

采集工具默认启用现有标定参数，无需额外添加参数：

```text
原始摄像头帧
  -> camera_calibration.npz 去畸变
  -> 按 --rotation 旋转
  -> 预览、图片保存和录像
```

仅在重新拍摄棋盘格、准备重新计算内参时关闭矫正：

```bash
python3 -m project.Tools.collect_samples \
  --device /dev/video0 \
  --label checkerboard_raw \
  --no-undistort \
  --count 20
```

### 11.4 本阶段验收条件

- 实际相机参数没有出现未处理的模式警告。
- 画面覆盖完整摆杆，±12 cm 附近仍可看到完整钢球。
- 空槽以及 `-10、-5、0、+5、+10 cm` 每个位置至少有 20 张清晰图片。
- 每个位置包含不同光照和轻微车体振动条件。
- 至少有一段钢球从一端滚到另一端的完整视频。
- 检查 `session.json` 与 `samples.jsonl` 中的厘米标签无误。
- 连续采集时画面延迟不随运行时间持续增加。

完成这些样本后，下一阶段才能基于真实图像实现 `ball_detector.py` 和 `rod_mapper.py`，而不是预先猜测钢球阈值和映射参数。

## 12. 推荐实施顺序

1. **固定机械与光学条件**：完成摄像头支架、视场、曝光、补光，保证整根摆杆始终清晰可见。
2. **完成静态标定**：验证 O、±5 cm、±10 cm 的坐标误差，目标是视觉测量误差远小于 1 cm，建议做到 ≤0.2 cm。
3. **实现单帧检测**：收集不同光照、不同位置的图片，确保全行程可检测。
4. **加入时序滤波和丢球保护**：记录原始值、滤波值、延迟和置信度。
5. **接入静止摆杆控制**：先完成 O → +5 cm → -5 cm，再实现任意目标坐标。
6. **进行车载动态测试**：依次测试直线加减速、半圆弯道、整圈，重点观察振动和光照变化。
7. **完善图传录像**：验证接收端位于赛道外时仍能实时观看，并逐个回放完整测试视频。
8. **做极限与故障测试**：低电量、网络断开、短时遮挡、摄像头掉帧、强反光和重新启动。

## 13. 验收清单

- [ ] 摄像头画面覆盖完整 25 cm 摆杆和钢球全行程。
- [ ] 钢球位置检测确实由摄像头完成。
- [ ] 摄像头不参与小车循迹；循迹仅使用红外光电模块。
- [ ] O、+5 cm、-5 cm 和任意位置都使用同一连续厘米坐标输出。
- [ ] 静态全量程视觉误差建议 ≤0.2 cm，明显小于题目 ±1 cm 指标。
- [ ] 动态检测帧率稳定，控制链路不受网页客户端和录像影响。
- [ ] 弯道、振动、反光条件下不持续丢球或跳点。
- [ ] 丢球和过期帧有明确安全策略。
- [ ] 场外设备能实时显示，且每次测试的视频均完整保存、可回放。
- [ ] 回传画面能直接判定钢球位置，并显示时间戳和检测状态。

## 14. 最容易失分的点

1. 用摄像头做循迹，违反“循迹模块只能使用红外光电模块”的限制。
2. 图传能看但不能保存，或视频文件无法完整回放。
3. 摄像头只做展示，实际位置检测用了其他传感器，违反强制要求。
4. 画面只拍到局部摆杆，钢球接近两端时出画。
5. 只对 O、±5 cm 写死阈值，无法完成任意指定位置控制。
6. 网络编码与控制串行，连接变慢后控制延迟骤增。
7. 固定阈值在钢球反光或环境光变化后误检，却没有置信度和丢球保护。
8. 静态效果正常，但未在车体转弯、加减速和振动条件下验证。

总的工程判断是：这道题的视觉难点不在复杂识别，而在于**稳定的一维厘米级测量、低延迟闭环、动态车载环境下不丢球，以及独立可靠的图传录像**。先把相机固定、曝光和标定做好，再写检测算法，成功率会明显高于单纯在软件中不断调阈值。
